import React, { Component } from "react";
import { View, Text, StyleSheet } from "react-native";

class AddNewDeck extends Component {
  render() {
    return (
      <View>
        <Text>Add a new deck here</Text>
      </View>
    );
  }
}

export default AddNewDeck;
